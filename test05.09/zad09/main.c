#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a = 8;
    if (0 < a < 5)
    {
        printf("%d is in the range. \n", a);
    }
    else
    {
        printf("%d is not in the range. \n", a);
    }
}
