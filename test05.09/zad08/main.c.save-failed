#include <stdio.h>
#include <stdlib.h>

int main()
{
    char arr[] = {1, 2, 3};
    char* p = arr;
    printf("%d\n%d\n", &arr[0], arr);
    if (p == &arr)
    {
        printf("Same");
    }
    else
    {
        printf("Not same");
    }
    getchar();
}
